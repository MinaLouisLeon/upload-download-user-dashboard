//navReducer
export const actionSetNav = (args) => {
    return{
        type : "setNav",
        payload : args
    }
}

// userInfoReducer
export const actionSetUserInfo = (args) => {
    return{
        type : "setUserInfo",
        payload : args
    }
}